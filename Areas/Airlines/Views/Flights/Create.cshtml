@model GroupOneFlight.Areas.Airlines.ViewModels.DetailFlightViewModel
@using GroupOneFlight.Areas.Airlines.Models
@{
    Layout = "~/Areas/Airlines/Views/Shared/_AirlineLayout.cshtml";
    ViewData["Title"] = "Create Flight";
    var today       = DateTime.Today.ToString("yyyy-MM-dd");
    var defaultDate = (Model.Flight != null && Model.Flight.Date > DateTime.MinValue)
                      ? Model.Flight.Date.ToString("yyyy-MM-dd") : today;
}

<div class="container py-5">
    <h2>Create New Flight</h2>
    <hr />

    <div class="row">
        <div class="col-md-8">
            <form method="post" action="@Url.Action("Create", "Flights", new { area = "Airlines" })">
                @Html.AntiForgeryToken()

                @if (!ViewData.ModelState.IsValid)
                {
                    <div class="alert alert-danger">
                        <strong>Validation Errors:</strong>
                        <ul class="mb-0">
                            @foreach (var error in ViewData.ModelState.Values.SelectMany(v => v.Errors))
                            {
                                <li>@error.ErrorMessage</li>
                            }
                        </ul>
                    </div>
                }

                <div class="form-group mb-3">
                    <label class="form-label">Flight Number <span class="text-danger">*</span></label>
                    <input type="text" class="form-control" name="FlightNumber"
                           placeholder="e.g., UA123" value="@Model.Flight?.FlightNumber" required />
                </div>

                <div class="form-group mb-3">
                    <label class="form-label">Airline <span class="text-danger">*</span></label>
                    <select class="form-select" name="AirlineId" required>
                        <option value="">Select Airline</option>
                        @foreach (var airline in Model.Airlines ?? new List<Airline>())
                        {
                            if (Model.Flight?.AirlineId == airline.Id)
                            {
                                <option value="@airline.Id" selected="selected">@airline.Name</option>
                            }
                            else
                            {
                                <option value="@airline.Id">@airline.Name</option>
                            }
                        }
                    </select>
                </div>

                <div class="row">
                    <div class="col-md-6">
                        <div class="form-group mb-3">
                            <label class="form-label">From <span class="text-danger">*</span></label>
                            <input type="text" class="form-control" name="From"
                                   placeholder="e.g., New York" value="@Model.Flight?.From" required />
                        </div>
                    </div>
                    <div class="col-md-6">
                        <div class="form-group mb-3">
                            <label class="form-label">To <span class="text-danger">*</span></label>
                            <input type="text" class="form-control" name="To"
                                   placeholder="e.g., Los Angeles" value="@Model.Flight?.To" required />
                        </div>
                    </div>
                </div>

                <div class="row">
                    <div class="col-md-6">
                        <div class="form-group mb-3">
                            <label class="form-label">Date <span class="text-danger">*</span></label>
                            <input type="date" class="form-control" name="Date"
                                   value="@defaultDate" min="@today" required />
                        </div>
                    </div>
                    <div class="col-md-6">
                        <div class="form-group mb-3">
                            <label class="form-label">Cabin Type <span class="text-danger">*</span></label>
                            <select class="form-select" name="CabinType" required>
                                <option value="">Select Cabin Type</option>
                                @foreach (var cabin in Model.CabinTypes ?? new List<string>())
                                {
                                    if (Model.Flight?.CabinType == cabin)
                                    {
                                        <option value="@cabin" selected="selected">@cabin</option>
                                    }
                                    else
                                    {
                                        <option value="@cabin">@cabin</option>
                                    }
                                }
                            </select>
                        </div>
                    </div>
                </div>

                <div class="row">
                    <div class="col-md-6">
                        <div class="form-group mb-3">
                            <label class="form-label">Departure Time <span class="text-danger">*</span></label>
                            <input type="time" class="form-control" name="DepartureTime"
                                   value="@Model.Flight?.DepartureTime" required />
                        </div>
                    </div>
                    <div class="col-md-6">
                        <div class="form-group mb-3">
                            <label class="form-label">Arrival Time <span class="text-danger">*</span></label>
                            <input type="time" class="form-control" name="ArrivalTime"
                                   value="@Model.Flight?.ArrivalTime" required />
                        </div>
                    </div>
                </div>

                <div class="form-group mb-3">
                    <label class="form-label">Aircraft Type <span class="text-danger">*</span></label>
                    <select class="form-select" name="AircraftType" required>
                        <option value="">Select Aircraft Type</option>
                        @foreach (var aircraft in Model.AircraftTypes ?? new List<string>())
                        {
                            if (Model.Flight?.AircraftType == aircraft)
                            {
                                <option value="@aircraft" selected="selected">@aircraft</option>
                            }
                            else
                            {
                                <option value="@aircraft">@aircraft</option>
                            }
                        }
                    </select>
                </div>

                <div class="row">
                    <div class="col-md-6">
                        <div class="form-group mb-3">
                            <label class="form-label">CO2 Emission (kg) <span class="text-danger">*</span></label>
                            <input type="number" class="form-control" name="Emission"
                                   step="0.01" min="0" placeholder="e.g., 85.50"
                                   value="@(Model.Flight?.Emission > 0 ? Model.Flight.Emission.ToString("0.00") : "")"
                                   required />
                        </div>
                    </div>
                    <div class="col-md-6">
                        <div class="form-group mb-3">
                            <label class="form-label">Price (USD) <span class="text-danger">*</span></label>
                            <div class="input-group">
                                <span class="input-group-text">$</span>
                                <input type="number" class="form-control" name="Price"
                                       step="0.01" min="0" placeholder="e.g., 299.00"
                                       value="@(Model.Flight?.Price > 0 ? Model.Flight.Price.ToString("0.00") : "")"
                                       required />
                            </div>
                        </div>
                    </div>
                </div>

                <div class="form-group mb-3 d-flex gap-2">
                    <button type="submit" class="btn btn-success">
                        <i class="fas fa-save me-1"></i> Create Flight
                    </button>
                    <a href="@Url.Action("Manage")" class="btn btn-secondary">
                        <i class="fas fa-times me-1"></i> Cancel
                    </a>
                </div>

            </form>
        </div>

        <div class="col-md-4">
            <div class="card bg-light">
                <div class="card-body">
                    <h5 class="card-title"><i class="fas fa-info-circle me-1"></i> Tips</h5>
                    <ul class="small mb-0">
                        <li>Flight numbers should be unique (e.g., UA123)</li>
                        <li>Date must be after today and within 3 years</li>
                        <li>Ensure departure time is before arrival time</li>
                        <li>Emission is the estimated CO2 in kilograms</li>
                        <li>All fields marked with <span class="text-danger">*</span> are required</li>
                    </ul>
                </div>
            </div>
        </div>
    </div>
</div>
